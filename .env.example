# API Configuration
REACT_APP_API_BASE_URL=http://localhost:8000

# Environment
REACT_APP_ENV=development

# AWS Cognito Configuration
REACT_APP_COGNITO_REGION=us-east-1
REACT_APP_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
REACT_APP_COGNITO_CLIENT_ID=your-client-id-here
# IMPORTANT: Do NOT include https:// in the domain
REACT_APP_COGNITO_DOMAIN=your-domain.auth.us-east-1.amazoncognito.com

# OAuth Redirect URLs (usually your app URL)
REACT_APP_COGNITO_REDIRECT_SIGNIN=http://localhost:3000
REACT_APP_COGNITO_REDIRECT_SIGNOUT=http://localhost:3000

# SAML Identity Provider Name (IMPORTANT: Must match exactly the name in Cognito User Pool)
# To find this: Go to AWS Console -> Cognito User Pool -> Sign-in experience -> Federated identity providers
# Common names: IdentityCenter, AWSIdentityCenter, SAML, or your custom name
REACT_APP_COGNITO_IDENTITY_PROVIDER=IdentityCenter

# Default AWS Account (optional - for development only)
# REACT_APP_DEFAULT_ACCOUNT_ID=
# REACT_APP_DEFAULT_ACCOUNT_NAME=
